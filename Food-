```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <meta
    name="description"
    content="OK Fastfood - Fresh, delicious and affordable fast food."
  >

  <meta name="theme-color" content="#ff5a1f">

  <title>OK Fastfood | Fresh • Fast • Delicious</title>

  <style>
    /* =====================================================
       OK FASTFOOD
       EASY CUSTOMIZATION
       ===================================================== */

    :root {
      --primary: #ff5a1f;
      --secondary: #ffb703;
      --dark: #0b0b0b;
      --card: #151515;
      --white: #ffffff;
      --muted: #a8a8a8;
      --border: rgba(255, 255, 255, 0.09);
      --max-width: 1200px;
    }

    /* =====================================================
       RESET
       ===================================================== */

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family:
        Inter,
        Arial,
        Helvetica,
        sans-serif;

      background: var(--dark);
      color: var(--white);
      line-height: 1.6;
      overflow-x: hidden;
    }

    img {
      display: block;
      width: 100%;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    button {
      font: inherit;
    }

    .container {
      width: min(92%, var(--max-width));
      margin: auto;
    }

    /* =====================================================
       LOADING SCREEN
       ===================================================== */

    .loader {
      position: fixed;
      inset: 0;
      background: var(--dark);
      z-index: 9999;

      display: flex;
      align-items: center;
      justify-content: center;

      transition: opacity 0.5s ease, visibility 0.5s ease;
    }

    .loader.hidden {
      opacity: 0;
      visibility: hidden;
    }

    .loader-logo {
      font-size: 2rem;
      font-weight: 900;
      animation: loaderPulse 1s infinite alternate;
    }

    .loader-logo span {
      color: var(--primary);
    }

    @keyframes loaderPulse {
      from {
        transform: scale(0.95);
        opacity: 0.6;
      }

      to {
        transform: scale(1.05);
        opacity: 1;
      }
    }

    /* =====================================================
       NAVBAR
       ===================================================== */

    .navbar {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;

      z-index: 1000;

      background: rgba(11, 11, 11, 0.75);
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);

      border-bottom: 1px solid var(--border);
    }

    .nav-inner {
      min-height: 74px;

      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo {
      font-size: 1.45rem;
      font-weight: 900;
      letter-spacing: -0.5px;
    }

    .logo span {
      color: var(--primary);
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 28px;

      list-style: none;
    }

    .nav-links a {
      color: #ddd;
      font-size: 0.95rem;
      transition: color 0.25s ease;
    }

    .nav-links a:hover {
      color: var(--primary);
    }

    .nav-order {
      background: var(--primary);
      padding: 11px 19px;
      border-radius: 30px;

      font-weight: 700;

      transition:
        transform 0.25s ease,
        box-shadow 0.25s ease;
    }

    .nav-order:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(255, 90, 31, 0.3);
    }

    .menu-toggle {
      display: none;

      border: 0;
      background: transparent;
      color: white;

      font-size: 1.7rem;
      cursor: pointer;
    }

    /* =====================================================
       HERO
       ===================================================== */

    .hero {
      min-height: 100vh;

      position: relative;
      overflow: hidden;

      display: flex;
      align-items: center;

      padding: 130px 0 80px;
    }

    .hero::before {
      content: "";

      position: absolute;

      width: 500px;
      height: 500px;

      background: var(--primary);

      border-radius: 50%;

      filter: blur(180px);
      opacity: 0.16;

      right: -180px;
      top: 80px;

      pointer-events: none;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: 1.05fr 0.95fr;

      align-items: center;

      gap: 60px;
    }

    .hero-content {
      animation: fadeUp 0.9s ease both;
    }

    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;

      padding: 8px 14px;

      border: 1px solid rgba(255, 90, 31, 0.35);
      background: rgba(255, 90, 31, 0.09);

      color: var(--primary);

      border-radius: 50px;

      font-size: 0.85rem;
      font-weight: 700;

      margin-bottom: 22px;
    }

    .hero h1 {
      font-size: clamp(3rem, 6vw, 5.8rem);

      line-height: 0.98;
      letter-spacing: -3px;

      margin-bottom: 24px;
    }

    .hero h1 span {
      color: var(--primary);
    }

    .hero-description {
      max-width: 590px;

      color: var(--muted);

      font-size: 1.05rem;

      margin-bottom: 30px;
    }

    .hero-buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
    }

    .btn {
      display: inline-flex;

      align-items: center;
      justify-content: center;

      min-height: 50px;

      padding: 0 23px;

      border-radius: 50px;

      font-weight: 800;

      transition:
        transform 0.25s ease,
        box-shadow 0.25s ease,
        background 0.25s ease;
    }

    .btn-primary {
      background: var(--primary);
      color: white;
    }

    .btn-primary:hover {
      transform: translateY(-4px);
      box-shadow: 0 15px 35px rgba(255, 90, 31, 0.35);
    }

    .btn-secondary {
      border: 1px solid var(--border);
      color: white;
    }

    .btn-secondary:hover {
      background: white;
      color: black;
      transform: translateY(-4px);
    }

    /* HERO FOOD */

    .hero-visual {
      display: flex;
      justify-content: center;

      animation: floating 4s ease-in-out infinite;
    }

    .food-wrapper {
      position: relative;

      width: min(430px, 90vw);
      aspect-ratio: 1 / 1;

      display: flex;
      align-items: center;
      justify-content: center;
    }

    .food-circle {
      position: absolute;

      width: 78%;
      aspect-ratio: 1;

      border-radius: 50%;

      background:
        radial-gradient(
          circle at 30% 30%,
          #ffcf5c,
          var(--primary) 45%,
          #c82d00 100%
        );

      box-shadow:
        0 35px 90px rgba(255, 90, 31, 0.22);
    }

    .food-image {
      position: relative;
      z-index: 2;

      width: 92%;
      aspect-ratio: 1;

      object-fit: cover;

      border-radius: 50%;

      border: 10px solid rgba(255, 255, 255, 0.08);

      box-shadow:
        0 30px 60px rgba(0, 0, 0, 0.55);
    }

    .floating-price {
      position: absolute;

      z-index: 5;

      right: 0;
      bottom: 12%;

      background: var(--white);
      color: var(--dark);

      padding: 13px 18px;

      border-radius: 16px;

      font-weight: 900;

      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.35);

      animation: smallFloat 3s ease-in-out infinite;
    }

    /* =====================================================
       TRUST FEATURES
       ===================================================== */

    .features {
      padding: 0 0 90px;
    }

    .feature-grid {
      display: grid;

      grid-template-columns: repeat(3, 1fr);

      gap: 18px;
    }

    .feature-card {
      background: var(--card);

      border: 1px solid var(--border);

      border-radius: 20px;

      padding: 27px;

      transition:
        transform 0.3s ease,
        border-color 0.3s ease;
    }

    .feature-card:hover {
      transform: translateY(-7px);

      border-color: rgba(255, 90, 31, 0.4);
    }

    .feature-icon {
      font-size: 1.7rem;
      margin-bottom: 12px;
    }

    .feature-card h3 {
      margin-bottom: 7px;
    }

    .feature-card p {
      color: var(--muted);
      font-size: 0.92rem;
    }

    /* =====================================================
       SECTIONS
       ===================================================== */

    .section {
      padding: 100px 0;
    }

    .section-header {
      text-align: center;

      max-width: 700px;

      margin: 0 auto 50px;
    }

    .eyebrow {
      color: var(--primary);

      font-size: 0.8rem;

      font-weight: 900;

      letter-spacing: 2px;

      text-transform: uppercase;
    }

    .section-header h2 {
      font-size: clamp(2.2rem, 5vw, 3.6rem);

      line-height: 1.05;

      margin: 10px 0 15px;
    }

    .section-header p {
      color: var(--muted);
    }

    /* =====================================================
       MENU
       ===================================================== */

    .menu-grid {
      display: grid;

      grid-template-columns:
        repeat(auto-fit, minmax(240px, 1fr));

      gap: 22px;
    }

    .menu-card {
      background: var(--card);

      border: 1px solid var(--border);

      border-radius: 22px;

      overflow: hidden;

      transition:
        transform 0.35s ease,
        box-shadow 0.35s ease;
    }

    .menu-card:hover {
      transform: translateY(-9px);

      box-shadow:
        0 25px 60px rgba(0, 0, 0, 0.35);
    }

    .menu-image {
      height: 210px;
      overflow: hidden;
    }

    .menu-image img {
      width: 100%;
      height: 100%;

      object-fit: cover;

      transition: transform 0.5s ease;
    }

    .menu-card:hover .menu-image img {
      transform: scale(1.08);
    }

    .menu-info {
      padding: 22px;
    }

    .menu-info h3 {
      margin-bottom: 8px;
      font-size: 1.15rem;
    }

    .menu-info p {
      color: var(--muted);

      font-size: 0.9rem;

      min-height: 45px;

      margin-bottom: 17px;
    }

    .menu-bottom {
      display: flex;

      align-items: center;
      justify-content: space-between;
    }

    .price {
      color: var(--secondary);

      font-size: 1.15rem;

      font-weight: 900;
    }

    .order-small {
      background: var(--primary);

      border: 0;

      color: white;

      padding: 9px 15px;

      border-radius: 30px;

      font-weight: 800;

      cursor: pointer;

      transition: transform 0.2s ease;
    }

    .order-small:hover {
      transform: scale(1.05);
    }

    /* =====================================================
       ABOUT
       ===================================================== */

    .about-section {
      background: #111111;
    }

    .about-grid {
      display: grid;

      grid-template-columns: 1fr 1fr;

      align-items: center;

      gap: 65px;
    }

    .about-image {
      position: relative;
    }

    .about-image img {
      border-radius: 25px;

      aspect-ratio: 4 / 3;

      object-fit: cover;

      border: 1px solid var(--border);
    }

    .about-content h2 {
      font-size: clamp(2.3rem, 5vw, 4rem);

      line-height: 1.05;

      margin: 12px 0 20px;
    }

    .about-content h2 span {
      color: var(--primary);
    }

    .about-content p {
      color: var(--muted);

      margin-bottom: 18px;
    }

    /* =====================================================
       CTA
       ===================================================== */

    .cta {
      padding: 100px 0;
    }

    .cta-box {
      position: relative;

      overflow: hidden;

      text-align: center;

      padding: 75px 25px;

      border-radius: 30px;

      background:
        linear-gradient(
          135deg,
          rgba(255, 90, 31, 0.18),
          rgba(255, 183, 3, 0.07)
        );

      border: 1px solid rgba(255, 90, 31, 0.2);
    }

    .cta-box::before {
      content: "";

      position: absolute;

      width: 300px;
      height: 300px;

      background: var(--primary);

      filter: blur(140px);

      opacity: 0.12;

      top: -150px;
      left: 50%;

      transform: translateX(-50%);
    }

    .cta-box > * {
      position: relative;
      z-index: 2;
    }

    .cta-box h2 {
      font-size: clamp(2.3rem, 5vw, 4rem);

      line-height: 1;

      margin-bottom: 18px;
    }

    .cta-box p {
      color: var(--muted);

      margin-bottom: 28px;
    }

    /* =====================================================
       CONTACT
       ===================================================== */

    .contact-grid {
      display: grid;

      grid-template-columns: repeat(2, 1fr);

      gap: 20px;
    }

    .contact-card {
      background: var(--card);

      border: 1px solid var(--border);

      padding: 30px;

      border-radius: 22px;
    }

    .contact-icon {
      font-size: 1.8rem;

      margin-bottom: 10px;
    }

    .contact-card h3 {
      margin-bottom: 5px;
    }

    .contact-card p {
      color: var(--muted);
    }

    .contact-card a {
      color: var(--primary);

      font-weight: 800;
    }

    /* =====================================================
       FOOTER
       ===================================================== */

    footer {
      border-top: 1px solid var(--border);

      padding: 30px 0;

      color: var(--muted);

      text-align: center;

      font-size: 0.9rem;
    }

    /* =====================================================
       FLOATING CALL BUTTON
       ===================================================== */

    .floating-call {
      position: fixed;

      right: 20px;
      bottom: 20px;

      z-index: 999;

      width: 60px;
      height: 60px;

      display: flex;

      align-items: center;
      justify-content: center;

      border-radius: 50%;

      background: var(--primary);

      color: white;

      font-size: 1.4rem;

      box-shadow:
        0 12px 35px rgba(255, 90, 31, 0.45);

      animation: pulse 2s infinite;
    }

    /* =====================================================
       ANIMATIONS
       ===================================================== */

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes floating {
      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-15px);
      }
    }

    @keyframes smallFloat {
      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-7px);
      }
    }

    @keyframes pulse {
      0% {
        box-shadow:
          0 0 0 0 rgba(255, 90, 31, 0.55);
      }

      70% {
        box-shadow:
          0 0 0 17px rgba(255, 90, 31, 0);
      }

      100% {
        box-shadow:
          0 0 0 0 rgba(255, 90, 31, 0);
      }
    }

    /* =====================================================
       MOBILE
       ===================================================== */

    @media (max-width: 850px) {

      .nav-links,
      .nav-order {
        display: none;
      }

      .menu-toggle {
        display: block;
      }

      .nav-links.mobile-active {
        display: flex;

        position: absolute;

        top: 74px;
        left: 0;

        width: 100%;

        flex-direction: column;

        padding: 25px;

        background: rgba(11, 11, 11, 0.98);

        border-bottom: 1px solid var(--border);
      }

      .hero {
        padding-top: 120px;
      }

      .hero-grid {
        grid-template-columns: 1fr;

        text-align: center;

        gap: 45px;
      }

      .hero-description {
        margin-left: auto;
        margin-right: auto;
      }

      .hero-buttons {
        justify-content: center;
      }

      .feature-grid {
        grid-template-columns: 1fr;
      }

      .about-grid {
        grid-template-columns: 1fr;

        gap: 40px;
      }

      .contact-grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 500px) {

      .hero h1 {
        letter-spacing: -2px;
      }

      .food-wrapper {
        width: 320px;
      }

      .floating-price {
        right: -5px;
      }

      .section {
        padding: 75px 0;
      }

      .cta {
        padding: 75px 0;
      }

      .cta-box {
        padding: 55px 20px;
      }

      .floating-call {
        width: 56px;
        height: 56px;

        right: 15px;
        bottom: 15px;
      }
    }

    /* =====================================================
       REDUCED MOTION
       ===================================================== */

    @media (prefers-reduced-motion: reduce) {

      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        scroll-behavior: auto !important;
        transition-duration: 0.01ms !important;
      }
    }
  </style>
</head>

<body>

  <!-- ===================================================
       LOADER
       =================================================== -->

  <div class="loader" id="loader">
    <div class="loader-logo">
      OK <span>FASTFOOD</span>
    </div>
  </div>


  <!-- ===================================================
       NAVBAR
       =================================================== -->

  <header class="navbar">

    <div class="container nav-inner">

      <a href="#home" class="logo">
        OK <span>FASTFOOD</span>
      </a>

      <button
        class="menu-toggle"
        id="menuToggle"
        type="button"
        aria-label="Open navigation menu"
        aria-expanded="false"
      >
        ☰
      </button>

      <ul class="nav-links" id="navLinks">

        <li>
          <a href="#home">Home</a>
        </li>

        <li>
          <a href="#menu">Menu</a>
        </li>

        <li>
          <a href="#about">About</a>
        </li>

        <li>
          <a href="#contact">Contact</a>
        </li>

      </ul>

      <a
        href="tel:+8801716861596"
        class="nav-order"
      >
        Order Now
      </a>

    </div>

  </header>


  <main>

    <!-- =================================================
         HERO
         ================================================= -->

    <section class="hero" id="home">

      <div class="container hero-grid">

        <div class="hero-content">

          <div class="badge">
            🔥 Fresh • Fast • Delicious
          </div>

          <h1>
            Good Food.
            <br>
            <span>Good Mood.</span>
          </h1>

          <p class="hero-description">
            Welcome to OK Fastfood. Enjoy delicious,
            fresh and satisfying fast food made for
            people who love great taste.
          </p>

          <div class="hero-buttons">

            <a
              href="tel:+8801716861596"
              class="btn btn-primary"
            >
              📞 Call & Order
            </a>

            <a
              href="#menu"
              class="btn btn-secondary"
            >
              Explore Menu
            </a>

          </div>

        </div>


        <div class="hero-visual">

          <div class="food-wrapper">

            <div class="food-circle"></div>

            <img
              class="food-image"
              src="https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&fit=crop&w=900&q=85"
              alt="Delicious burger from OK Fastfood"
              loading="eager"
            >

            <div class="floating-price">
              🍔 From ৳150
            </div>

          </div>

        </div>

      </div>

    </section>


    <!-- =================================================
         FEATURES
         ================================================= -->

    <section class="features">

      <div class="container feature-grid">

        <article class="feature-card">

          <div class="feature-icon">
            ⚡
          </div>

          <h3>Fast Service</h3>

          <p>
            Quick service so you don't have to wait
            too long for your favorite food.
          </p>

        </article>


        <article class="feature-card">

          <div class="feature-icon">
            🍔
          </div>

          <h3>Fresh & Tasty</h3>

          <p>
            Delicious food prepared with care and
            quality ingredients.
          </p>

        </article>


        <article class="feature-card">

          <div class="feature-icon">
            ❤️
          </div>

          <h3>Made For You</h3>

          <p>
            Great taste, friendly service and a
            memorable food experience.
          </p>

        </article>

      </div>

    </section>


    <!-- =================================================
         MENU
         ================================================= -->

    <section class="section" id="menu">

      <div class="container">

        <div class="section-header">

          <div class="eyebrow">
            Our Menu
          </div>

          <h2>
            Customer Favorites
          </h2>

          <p>
            Try some of our most popular and delicious
            fast food items.
          </p>

        </div>


        <div class="menu-grid">


          <!-- MENU 1 -->

          <article class="menu-card">

            <div class="menu-image">

              <img
                src="https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&fit=crop&w=800&q=85"
                alt="Classic Burger"
                loading="lazy"
              >

            </div>

            <div class="menu-info">

              <h3>
                Classic Burger
              </h3>

              <p>
                Juicy burger with fresh vegetables
                and delicious sauce.
              </p>

              <div class="menu-bottom">

                <span class="price">
                  ৳150
                </span>

                <a
                  href="tel:+8801716861596"
                  class="order-small"
                >
                  Order
                </a>

              </div>

            </div>

          </article>


          <!-- MENU 2 -->

          <article class="menu-card">

            <div class="menu-image">

              <img
                src="https://images.unsplash.com/photo-1513104890138-7c749659a591?auto=format&fit=crop&w=800&q=85"
                alt="Cheese Pizza"
                loading="lazy"
              >

            </div>

            <div class="menu-info">

              <h3>
                Cheese Pizza
              </h3>

              <p>
                Cheesy pizza loaded with tasty
                toppings.
              </p>

              <div class="menu-bottom">

                <span class="price">
                  ৳350
                </span>

                <a
                  href="tel:+8801716861596"
                  class="order-small"
                >
                  Order
                </a>

              </div>

            </div>

          </article>


          <!-- MENU 3 -->

          <article class="menu-card">

            <div class="menu-image">

              <img
                src="https://images.unsplash.com/photo-1573080496219-bb080dd4f877?auto=format&fit=crop&w=800&q=85"
                alt="Crispy French Fries"
                loading="lazy"
              >

            </div>

            <div class="menu-info">

              <h3>
                Crispy Fries
              </h3>

              <p>
                Golden crispy fries served hot and
                fresh.
              </p>

              <div class="menu-bottom">

                <span class="price">
                  ৳100
                </span>

                <a
                  href="tel:+8801716861596"
                  class="order-small"
                >
                  Order
                </a>

              </div>

            </div>

          </article>


          <!-- MENU 4 -->

          <article class="menu-card">

            <div class="menu-image">

              <img
                src="https://images.unsplash.com/photo-1550547660-d9450f859349?auto=format&fit=crop&w=800&q=85"
                alt="Chicken Burger"
                loading="lazy"
              >

            </div>

            <div class="menu-info">

              <h3>
                Chicken Burger
              </h3>

              <p>
                Crispy chicken burger with fresh
                vegetables and sauce.
              </p>

              <div class="menu-bottom">

                <span class="price">
                  ৳180
                </span>

                <a
                  href="tel:+8801716861596"
                  class="order-small"
                >
                  Order
                </a>

              </div>

            </div>

          </article>


        </div>

      </div>

    </section>


    <!-- =================================================
         ABOUT
         ================================================= -->

    <section class="section about-section" id="about">

      <div class="container about-grid">

        <div class="about-image">

          <img
            src="https://images.unsplash.com/photo-1552566626-52f8b828add9?auto=format&fit=crop&w=1000&q=85"
            alt="OK Fastfood restaurant"
            loading="lazy"
          >

        </div>


        <div class="about-content">

          <div class="eyebrow">
            About Us
          </div>

          <h2>
            Taste That
            <span>Makes You Smile.</span>
          </h2>

          <p>
            At OK Fastfood, our goal is simple:
            serve tasty food with fast and friendly
            service.
          </p>

          <p>
            Whether you're craving a juicy burger,
            cheesy pizza or crispy fries, we're here
            to satisfy your hunger.
          </p>

          <a
            href="tel:+8801716861596"
            class="btn btn-primary"
          >
            📞 Order Your Food
          </a>

        </div>

      </div>

    </section>


    <!-- =================================================
         CTA
         ================================================= -->

    <section class="cta">

      <div class="container">

        <div class="cta-box">

          <h2>
            Hungry Right Now?
          </h2>

          <p>
            Don't wait. Call OK Fastfood and place
            your order today.
          </p>

          <a
            href="tel:+8801716861596"
            class="btn btn-primary"
          >
            📞 01716-861596
          </a>

        </div>

      </div>

    </section>


    <!-- =================================================
         CONTACT
         ================================================= -->

    <section class="section" id="contact">

      <div class="container">

        <div class="section-header">

          <div class="eyebrow">
            Contact
          </div>

          <h2>
            Visit or Call Us
          </h2>

          <p>
            Ready for something delicious?
            Get in touch with OK Fastfood.
          </p>

        </div>


        <div class="contact-grid">

          <article class="contact-card">

            <div class="contact-icon">
              📞
            </div>

            <h3>
              Call Us
            </h3>

            <p>
              For orders and inquiries
            </p>

            <br>

            <a href="tel:+8801716861596">
              01716-861596
            </a>

          </article>


          <article class="contact-card">

            <div class="contact-icon">
              📍
            </div>

            <h3>
              Location
            </h3>

            <p>
              85C9+7JH
            </p>

          </article>

        </div>

      </div>

    </section>

  </main>


  <!-- ===================================================
       FOOTER
       =================================================== -->

  <footer>

    <div class="container">

      <p>
        © <span id="year"></span>
        OK Fastfood. All Rights Reserved.
      </p>

    </div>

  </footer>


  <!-- ===================================================
       FLOATING CALL BUTTON
       =================================================== -->

  <a
    href="tel:+8801716861596"
    class="floating-call"
    aria-label="Call OK Fastfood"
  >
    📞
  </a>


  <!-- ===================================================
       JAVASCRIPT
       =================================================== -->

  <script>

    /* ================================
       LOADER
       ================================ */

    window.addEventListener("load", function () {

      const loader = document.getElementById("loader");

      setTimeout(function () {

        loader.classList.add("hidden");

      }, 500);

    });


    /* ================================
       MOBILE MENU
       ================================ */

    const menuToggle =
      document.getElementById("menuToggle");

    const navLinks =
      document.getElementById("navLinks");


    menuToggle.addEventListener("click", function () {

      const isOpen =
        navLinks.classList.toggle("mobile-active");

      menuToggle.setAttribute(
        "aria-expanded",
        isOpen
      );

    });


    /* ================================
       CLOSE MOBILE MENU AFTER CLICK
       ================================ */

    document
      .querySelectorAll(".nav-links a")
      .forEach(function (link) {

        link.addEventListener("click", function () {

          navLinks.classList.remove(
            "mobile-active"
          );

          menuToggle.setAttribute(
            "aria-expanded",
            "false"
          );

        });

      });


    /* ================================
       CURRENT YEAR
       ================================ */

    document.getElementById("year").textContent =
      new Date().getFullYear();

  </script>

</body>
</html>
```
